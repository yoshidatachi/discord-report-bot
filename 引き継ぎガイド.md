# 引き継ぎガイド(初めての人向け)

このドキュメントは、プログラミングに詳しくない人が「このBotを引き継いだけど何をすればいいか分からない」となった時のためのものです。技術的な仕様やコマンドの意味は `README.md` に詳しく書いてあるので、ここでは**そもそも何なのか**、**今どういう状態で動いているか**、**困った時にどうするか**を中心に、順を追って説明します。

上から順番に読んでいけば、初めての人でも対応できるようになるはずです。

---

## 1. これは何?

Discord上で動いている「自動でお手伝いをするプログラム」です。人間ではなく、常にどこかのパソコン上で動き続けているソフトウェアです。

このBotは2つのことをしています。

1. **`/report` というコマンド**を打つと、報告会用のテンプレートを自動で作ってくれる(PDFを指定すれば中身も自動で読み取ってくれる)
2. **`#報告資料` チャンネルにPDFが投稿されると**、内容を読み取って `議事録` フォーラムの該当者のスレッドに自動でPDFとテンプレートを追加してくれる

細かい仕様は `README.md` にまとまっています。

---

## 2. 今の運用環境について

現在、このBotは以下の組み合わせで動いています。

- **研究室のPC(Windows 10)** の上で動いている
- **`pm2`** というツールを使い、ターミナルの画面を閉じてもBotが止まらないようにしている
- そのPCは **自動ログイン** が設定されており、PCを再起動しても人の手を借りずにBotが自動的に立ち上がるようになっている

つまり、普段は「誰かが何か操作しないといけない」場面はほとんどありません。ただし、前任者から**そのPCが物理的にどこにあるか**は必ず確認しておいてください。トラブルが起きた時に触りに行く必要があります。

---

## 3. 動いているか確認する方法

Discordのメンバー一覧、またはBotのアイコンの右下の丸を見てください。

- 🟢 緑の丸 = 動いている(オンライン)
- ⚫ 灰色の丸 = 止まっている(オフライン)

オフラインになっている場合、`/report` を打っても何も反応しませんし、PDFを投稿しても議事録スレッドへの自動反映も起きません。

より詳しく確認したい場合は、研究室のPCでターミナルを開き、以下を打ちます。

```bash
pm2 status
```

`discord-report-bot` の行の `status` 列が `online` になっていれば正常です。

---

## 4. Botが止まっていた時にやること

研究室のPCの前に行き、ターミナル(黒い画面。コマンドプロンプトやPowerShell)を開いてください。

```bash
pm2 status
```

- `online` と表示されている → Botは動いています。それでも反応がない場合は7章のFAQを確認してください
- `stopped` や `errored` と表示されている、またはそもそも一覧に無い → 止まっています。以下で再起動します

```bash
pm2 restart discord-report-bot
```

一覧に無い場合(pm2が初めて起動した、設定が消えた等)は、プロジェクトのフォルダに移動してから起動し直します。

```bash
cd discord-report-bot
pm2 start index.js --name discord-report-bot
pm2 save
```

`pm2 save` を忘れると、次回PC再起動時に自動で立ち上がらなくなるので必ず実行してください。

エラーが出て起動しない場合は、慌てずに以下を確認してください。

- `.env` というファイルがちゃんと存在するか(消えていないか)
- `npm install` を一度実行し忘れていないか
- インターネットに繋がっているか

それでも直らない場合は、エラーメッセージをそのままコピーして、詳しい人(先輩・研究室のメンバーなど)に相談してください。エラーメッセージは重要な手がかりなので、一部を省略せずそのまま伝えるのがコツです。

---

## 5. もしもゼロから環境構築が必要になったら

PCが故障して買い替えた、PCを初期化した、など「今動いている環境が無くなってしまった」場合は、新しい環境にBotを一から構築し直す必要があります。手順は `README.md` の 1〜7章に詳しく書いてありますが、ここでは「何のためにその作業をするのか」を簡単に説明しておきます。

| README.mdの章 | やること | 何のため |
|---|---|---|
| 1. Node.jsをインストール | プログラムを動かす土台を入れる | Botのプログラム自体がNode.js製のため |
| 2. Discord Developer Portal | Bot用のアカウントを作り、トークン(合言葉)を発行する | Discordと通信するための身分証を作る |
| 3. 各種IDを控える | サーバー・チャンネルのIDをメモする | Botに「どこで動くか」を教えるため |
| 4. プロジェクトファイルを配置 | コードを置き、必要な部品を揃える | プログラム本体の準備 |
| 5. `.env`ファイルを作成 | トークンなどの設定を書き込む | Botに合言葉と設定を教える |
| 6. スラッシュコマンドを登録 | `/report` をDiscordに認識させる | コマンドを使えるようにする |
| 7. Botを起動 | 実際に動かす | 完了 |

このあと、本ガイドの6章(pm2の設定)と8章(自動ログインの設定)も忘れずに行ってください。これをやらないと、PCを再起動するたびに手動でBotを起動し直す必要が出てきます。

**トークンについての注意**: もし前任者からトークンをそのまま引き継ぐ場合、セキュリティ的にはあまり望ましくありません。可能であれば、引き継ぎのタイミングでDiscord Developer Portalの「Bot」ページから「Reset Token」を実行し、新しいトークンを発行し直すことをおすすめします。

---

## 6. pm2の使い方

`pm2` は、プログラムをバックグラウンドで動かし続けるためのツールです。これのおかげで、ターミナルの画面を閉じてもBotが止まりません。

### よく使うコマンド

| やりたいこと | コマンド |
|---|---|
| 動いているか確認する | `pm2 status` |
| Botを再起動する(コード更新後など) | `pm2 restart discord-report-bot` |
| Botを止める | `pm2 stop discord-report-bot` |
| Botの管理対象から外す | `pm2 delete discord-report-bot` |
| ログ(動作記録)を見る | `pm2 logs discord-report-bot` |

`pm2 logs` はCtrl+Cで見るのをやめられます(Bot自体は止まりません)。何か問題が起きた時、まずここを見るとヒントが見つかることが多いです。

### 初めてpm2でBotを起動する場合

```bash
cd discord-report-bot
npm install -g pm2
pm2 start index.js --name discord-report-bot
pm2 save
```

`pm2 save` を実行すると、今動いているBotの一覧が保存されます。PC再起動後に自動で復帰させるには、次の設定も必要です。

### PC再起動時に自動で立ち上げる設定(Windows)

```bash
:: 管理者として実行したコマンドプロンプトで
npm install -g pm2-windows-startup
pm2-startup install
pm2 save
```

**重要な注意点**: この設定は「Windowsに誰かがログインした時」に発動する仕組みです。PCの電源が入るだけでは足りず、そのアカウントに実際にログインするまでBotは起動しません。これをカバーするのが次の8章の「自動ログイン」設定です。

---

## 7. よくある「困った」と対処法

### Q. `/report` を打っても反応しない
→ まずBotがオンラインか確認(3章参照)。オンラインなのに反応しない場合は、コマンドの登録(`npm run deploy`)がまだされていない可能性があります。

### Q. PDFを投稿したのに議事録スレッドに反映されない
→ 以下を順番に確認してください。
- Botがオンラインか
- 投稿したチャンネルが `.env` の `REPORT_CHANNEL_ID` で指定したチャンネルと合っているか
- ファイルの拡張子が `.pdf` になっているか(`.PDF`など大文字は大丈夫ですが、`.docx`などは無視されます)

### Q. `議事録` フォーラムに `ERROR` という名前のスレッドに投稿された
→ これはBotが「このPDFのフォーマットが読み取れなかった」または「該当者のスレッドが見つからなかった」と判断した時の動作です。`ERROR` スレッドに投稿された内容を見て、本来誰の議事録なのか手動で該当者のスレッドにコピーしてください。頻発するようであれば、報告書のフォーマット自体を見直すか、詳しい人に相談してPDFの読み取りルールを調整してもらってください。

### Q. 種別(Gセミ/報告会)の判定が間違っている
→ Discordの仕様上、ファイル名の日本語部分が失われることがあり、これが原因で「グルセミ」のつもりが「報告会」と判定されてしまうことがあります(詳しくは `README.md` の9章)。**ファイル名の種別部分には「Gセミ」(半角の大文字G)を使うことを、研究室内で徹底することをおすすめします。** 半角/全角の「G」はこの現象の影響を受けにくいためです。

### Q. 該当者の議事録スレッドが元々存在しない(初めて報告する人など)
→ 現在の仕様では、新規スレッドは自動作成されません。上記と同様に `ERROR` スレッドに投稿されるので、手動で新しいスレッドを作成してタグ付けし、内容を移してください。

### Q. PCを再起動したらBotが自動で立ち上がっていない
→ 考えられる原因は3つです。
1. 再起動後、自動ログインされずパスワード入力画面で止まっている → 誰かがログインするまでpm2は動き出しません。ログインしてから `pm2 status` で確認してください
2. 自動ログインの設定が(Windowsアップデートなどで)解除されてしまった → 8章を参照して再設定してください
3. 前回 `pm2 save` を実行し忘れていた → 4章の手順でBotを起動し直し、必ず `pm2 save` まで実行してください

### Q. Botのトークンが漏れた/流出したかもしれない
→ すぐにDiscord Developer Portal(前任者から引き継いだアカウントでログイン)の「Bot」ページから「Reset Token」を実行してください。新しいトークンを `.env` の `DISCORD_TOKEN` に書き直し、Botを再起動(`pm2 restart discord-report-bot`)すれば元通り動きます。

---

## 8. Windowsでの自動ログイン設定

PCが再起動した際、パスワード入力なしで自動的にログインまで進むようにする設定です。これとpm2を組み合わせることで、PC再起動後もBotが自動的に立ち上がるようになります。

1. `Win + R` を押して `netplwiz` と入力しEnter
2. 「ユーザーアカウント」画面で、Botを動かすアカウントを選択
3. 「ユーザーがこのコンピューターを使うには、ユーザー名とパスワードを入力する必要があります」のチェックを**外す**
4. 「適用」を押すと、そのアカウントのパスワードを一度だけ聞かれるので入力してOK

設定後は**必ず実際にPCを再起動して確認**してください。パスワード入力画面が出ずに自動でデスクトップまで進み、`pm2 status` で `discord-report-bot` が `online` になっていれば成功です。

**セキュリティ上の注意**: 自動ログインを設定すると、そのPCの電源を入れられる人は誰でもそのアカウントの中身(ファイルなど)にアクセスできる状態になります。研究室内で完結していて、他の用途にも使う共用PCでなければ大きな問題にはならないことが多いですが、一応認識しておいてください。

---

## 9. さらに次の人へ引き継ぐ場合

今後もこのBotを使い続けるなら、以下をやっておくと後の人が楽になります。

- コード一式をGitHubなど、個人アカウントに紐づかない場所に保管する
- Discord Developer Portal側で「チーム」を作成し、Botをチーム所有にしておく(個人のアカウントに紐づいたままだと、そのアカウントが使えなくなった時に詰みます)
- この `引き継ぎガイド.md` と `README.md` をセットで渡す
- 可能であれば、実際に一緒に `/report` を打ってみせる、PDFを投稿してみせるなど、一度動かして見せておく
- Botを動かしているPCの場所と、そのPCへのログイン方法を伝えておく

---

## 10. 用語ミニ辞典

- **Bot**: Discord上で自動的に動くプログラムのこと。人間のアカウントではない
- **トークン**: Botの「パスワード」のようなもの。他人に見せたり、GitHubなどに公開したりしては絶対にダメ
- **スラッシュコマンド**: `/report` のように `/` から始まる、Discord上で打てるコマンド
- **フォーラムチャンネル**: 通常のチャンネルと違い、投稿ごとに個別の「スレッド」ができるタイプのチャンネル。今回の `議事録` チャンネルがこれにあたる
- **`.env` ファイル**: トークンなど、他人に見せてはいけない設定値を書いておくファイル
- **ターミナル/コマンドプロンプト**: 文字でパソコンに指示を出すための黒い画面のアプリ
- **Node.js**: このBotのプログラムを動かすために必要な土台となるソフト
- **pm2**: プログラムをバックグラウンドで動かし続けるためのツール。これのおかげでターミナルを閉じてもBotが止まらない
- **自動ログイン**: PCを起動した時に、パスワード入力なしで自動的にログインまで進む設定。これとpm2の組み合わせで、PC再起動後もBotが自動的に立ち上がるようになっている
